/**
 * Automations. Owns: no table yet; the schema lands with the automations
 * sub-project.
 *
 * Empty until the automations endpoints are implemented. Every query function
 * takes the principal's workspace id as its first argument and builds its
 * statement through the shared scoping helper, so no query reaches a tenant
 * table unscoped.
 */
