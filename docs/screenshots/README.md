# Screenshots

Three images referenced by the root `README.md`, captured against the live `/demo` deployment:

- `dashboard.png` — `/dashboard`
- `invoices.png` — `/invoices`
- `collections.png` — `/collections`

Not generated by this repository's tooling — capture them by hand once `/demo` is live in production, at roughly 1400px wide, light mode.
